version: '3.9'

services:
  server:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./server/data:/app/server/data
    environment:
      - PORT=3001
    restart: unless-stopped

  # Dev only: Vite dev server
  # client:
  #   build:
  #     context: ./client
  #     dockerfile: Dockerfile.dev
  #   ports:
  #     - "5173:5173"
